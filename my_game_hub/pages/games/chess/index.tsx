import Board from '@/components/Chess/Board';

const Chess = () => {
  return (
    <div style={{ backgroundColor: '#ccc', flex: 1, alignItems: 'center', justifyContent: 'center', display: 'flex' }}>
      <Board />
    </div>
  );
};

export default Chess;
